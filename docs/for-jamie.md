# For Jamie

## Active handoff - Run 271 - 27 August 2026

Run 271 publishes two more TRA-systems boundaries. Heritage rolling stock:
TRA's own internal regulation still formally manages 5 steam locomotives in
a tiered rental system (CT273/CK124 top tier, DT668/CK101, LDK59), with
running restrictions and a manufacturer-parts constraint on CK101; the
National Railway Museum's Phase 1 opened 31 July 2025 and is confirmed open
in 2026, no Phase 2 date found. Fire/evacuation: no currently-adopted
TRA-tunnel-specific fire regulation was confirmed to exist — a 2005 research
report explicitly says it's a proposal, not a standard; the 2021 Qingshui
Tunnel derailment's TTSB investigation found crew-training/documentation
gaps around on-board emergency equipment, but this page could not access
findings about the tunnel's own fixed infrastructure specifically.

Also fixed: the conflicts index needed regenerating twice more this batch
(now 61 records) — each new "Conflicts" section requires `npm run conflicts`
before the gate passes; `gate:fast` now runs this automatically since Run
269 so it's caught immediately rather than at the next full gate.

`npm run gate:fast` passed after fixes (8 uncited sentences across both
pages, found and sourced or reworded); `npm run research` clean (237 files,
992 checked failures). Font subset already covered the new CJK glyphs, no
regeneration needed this round. `probes/` remains untracked.

Next: continue TRA-systems/closed-line subjects via parallel scouts;
TRA-specific subjects are getting harder to find without duplicating
existing pages, so the next round should consider Parts 2-8 (ridership,
fare, TOD) more seriously.
