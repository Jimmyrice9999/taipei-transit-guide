# For Jamie

Run 52 continues. Part 1 batches 1-7 done (series-other/unclassified/700s,
series-100s/900s, series-300s, colour-green, colour-orange, trunk,
series-500s — 120 routes total). Full detail is in each batch's commit
message; see `git log --oneline` for "Run 52 — Part 1 batch N".

Trunk batch found a genuine open conflict: a currently-active route
(重慶幹線, 天母–東園) carries the same name route 300's own page says 300 held
before a 2017 rename. Published as unresolved on both pages, not settled.

series-500s batch: every one of the 20 routes has zero entries in the
curated MRT interchange set, however many stops are literally named for a
station — 513 and 508 each pass six such stops with zero confirmed joins,
the widest gap found so far. 508/508Shuttle/536/536Shuttle share nearly one
corridor at different lengths, described against what they actually share.

Gates: cite clean, test:unit 209/209, claims baseline held at 32, verify
running.

Next: series-0-99 (33 routes) is the next-smallest remaining group, then
colour-blue, colour-red, minibus, series-600s, special-shuttle, series-200s,
new-taipei (562). Parts 2-7 not started.
Do not poll Actions. Do not stage the pre-existing untracked `probes/` dir.
