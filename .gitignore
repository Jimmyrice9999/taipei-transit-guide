node_modules/
.next/
out/
next-env.d.ts
*.tsbuildinfo
.DS_Store
.env*.local

# Browser-verification artifacts. The reports and screenshots under docs/ ARE
# committed — they are review deliverables — but Playwright's own scratch
# output is not.
test-results/
playwright-report/
.playwright/
.image-cache/

# Agent tool-state. Local to whichever CLI/agent runtime is in use, not part
# of the project itself.
.agents/
.codex/
